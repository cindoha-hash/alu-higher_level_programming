The ``MyList`` class inherits from ``list``.

It provides one public instance method:

``print_sorted(self)``
    Prints a sorted copy of the list in ascending order without
    modifying the original list.
